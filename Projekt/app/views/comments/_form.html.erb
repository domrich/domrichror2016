<%= form_for([@gallery, @gallery.comments.build]) do |f| %>
	<%= f.label :name %><br>
	<%= f.text_field :name %><br>
	<br>
	<%= f.label :text %><br>
	<%= f.text_area :text %><br>
	<br>
	<%= f.submit %>
<% end %>
